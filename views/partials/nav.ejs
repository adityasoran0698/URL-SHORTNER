<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon/fonts/remixicon.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=DynaPuff:wght@400..700&display=swap');

        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        .logo {

            font-family: "DynaPuff", system-ui;
            font-optical-sizing: auto;
            font-weight: 500;
            font-style: normal;
            font-variation-settings:
                "wdth" 100;
            font-size: 32px;
            cursor: default;
        }



        .url {
            color: rgb(0, 0, 0);
            transition: all 0.3s ease-in;

        }

        .shortner {
            color: rgb(255, 0, 0);
            transition: all 0.3s ease-in;

        }



        .navbar {
            background: linear-gradient(45deg, rgb(78, 208, 255), rgb(80, 80, 255));
            padding: 10px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .navlists {
            display: flex;
            list-style: none;
            justify-content: space-between;
            align-items: center;
            padding: 10px;
            margin-left: 5px;

        }

        .navlists li {
            position: relative;


        }


        a {
            text-decoration: none;
            font-size: 27px;
            font-family: "Poppins", sans-serif;
            font-weight: 550;
            font-style: normal;
            color: #fefefe;
            display: flex;
            justify-content: center;
            align-items: center;
            transition: 0.5s;
            gap: 5px;
        }

        a:hover {
            color: darkblue;
            /* transform: scale(1.1); */
        }

        .navbar a:hover i {
            color: white;
        }

        @media (max-width: 768px) {
            .navlists {
                flex-direction: column;
                align-items: flex-start;
            }

            .dropdown-menu {
                left: auto;
                right: 0;
            }
        }

        .user-menu a:hover {
            cursor: pointer;
            color: darkblue;
        }

        .dropdown-menu {
            display: none;
            position: absolute;
            top: 100%;
            left: 0;
            background-color: rgb(255, 255, 255);
            border-radius: 10px;
            border: 1px solid #000;
            min-width: 100%;
            width: 110px;
            padding: 10px;
        }

        .dropdown-menu li {
            list-style: none;
            margin-bottom: 8px;
            background-color: #111827;
            padding: 5px;
            height: auto;
            border-radius: 10px;

        }

        .dropdown-menu a {
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 20px;
            font-weight: 600;
            color: rgb(255, 255, 255);
        }

        .dropdown-menu a:hover {
            color: rgb(255, 93, 93);
            ;
        }

        .navbar i {
            font-size: 23px;
            color: rgb(255, 50, 50);
            transition: all 0.3s ease-in;

        }

        #arrow-down {
            font-size: 35px;
            color: rgb(255, 255, 255);
        }
    </style>
</head>

<body>
    <div class="navbar">
        <nav>
            <ul class="navlists">
                <li class="logo"><span class="url">URL</span><span class="shortner">SHORTNER</span></li>
                <li><a href="/"></i>Home</a></li>
                <% if (locals.user) { %>
                    <li><a href="/newURL"></i>Generate URL</a></li>

                    <li class="user-menu"><a>
                            <%= user.name %> <i class="ri-arrow-drop-down-fill" id="arrow-down"></i>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a href="/user/logout"><i class="ri-logout-box-line"></i>Logout</a></li>
                        </ul>
                    </li>
                    <% } else { %>
                        <li><a href="/user/signup"><i class="ri-account-box-fill"></i></i>Create Account</a></li>


                        <li><a href="/login"><i class="ri-login-box-fill"></i></i>Signin</a></li>
                        <% } %>



            </ul>


        </nav>

    </div>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const username = document.querySelector(".user-menu");
            const dropdown = document.querySelector(".dropdown-menu");

            username.addEventListener("click", function (e) {
                e.stopPropagation(); // Prevent event bubbling
                dropdown.style.display = dropdown.style.display === "block" ? "none" : "block";
            });
            window.addEventListener("click", function (e) {
                e.stopPropagation();
                dropdown.style.display = "none";
            })


        });
    </script>
</body>


</html>